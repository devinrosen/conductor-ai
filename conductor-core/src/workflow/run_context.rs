// Intentionally empty — RunContext trait was removed as unused dead code.
// The module declaration is retained to avoid a churn PR; delete this file
// and the `pub(crate) mod run_context;` line in mod.rs when convenient.
