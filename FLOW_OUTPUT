## Summary

Implemented ticket #2850: replaced the concrete `ExecutionContext` and `ProviderContext` structs from runkon-flow's `ActionExecutor` and `ItemProvider` trait surfaces with a three-orthogonal-input shape.

## Changes

### runkon-flow (10 files)

- **`src/traits/run_context.rs`** — added `run_id()`, `workflow_name()`, `parent_run_id()`, `shutdown()` to `RunContext` trait; extended `NoopRunContext` with test-friendly defaults and builder methods
- **`src/traits/action_executor.rs`** — deleted `ExecutionContext`; introduced `StepInfo { step_id, step_timeout }`; updated `ActionExecutor::execute` and `ActionRegistry::dispatch` to three-input form; added `model`, `bot_name`, `plugin_dirs` to `ActionParams`
- **`src/traits/item_provider.rs`** — deleted `ProviderContext`; introduced `ProviderInfo { step_id }`; updated `ItemProvider::items` to drop `existing_set`
- **`src/test_helpers.rs`** — replaced `make_ectx()` with `make_run_ctx()` + `make_step_info()`; updated `make_params()` with new fields
- **`src/executors/mod.rs`** — replaced `build_execution_context` with `build_step_info`; extended `build_action_params` for model/bot_name/plugin_dirs pass-through
- **`src/executors/call.rs`** — updated dispatch to new four-arg form; updated in-file test executor
- **`src/executors/parallel.rs`** — updated `DispatchInput` to carry `Arc<dyn RunContext>` + `StepInfo`; updated all 8 test executors
- **`src/executors/foreach.rs`** — replaced `ProviderContext` with `ProviderInfo`; dropped `&existing_set` from provider call; engine-side dedup guard unchanged
- **`tests/common/mod.rs`** — updated `MockExecutor`, `FailingExecutor`, all `ItemProvider` impls to new signatures; providers now return all items
- **`tests/lease_refresh.rs`** — updated 3 test executors to new `execute` signature

### conductor-core (8 files)

- **`src/workflow/run_context.rs`** — `WorktreeRunContext` gains 4 new `RunContext` methods; constructor grows to 9 params; tests updated
- **`src/workflow/coordinator.rs`** — updated both `WorktreeRunContext::new(...)` call sites to pass `run_id`, `workflow_name`, `parent_run_id`, `shutdown`
- **`src/workflow/runkon_bridge.rs`** — `RkActionExecutorAdapter::execute` uses new three-input form; re-sourced `ExecutionContext` assembly from `ctx.*`/`info.*`/`params.*`; `delegate_items` drops `existing_set`; macro updated
- **`src/workflow/item_provider/mod.rs`** — `ItemProvider::items` drops `existing_set`; `collect_fan_out_items` simplified to 2-arg form
- **`src/workflow/item_provider/repos.rs`** — removed `existing_set`; updated tests
- **`src/workflow/item_provider/tickets.rs`** — removed `existing_set`; updated tests
- **`src/workflow/item_provider/workflow_runs.rs`** — removed `existing_set`; updated tests
- **`src/workflow/item_provider/worktrees.rs`** — removed `existing_set`; updated tests

### Documentation (1 file)

- **`docs/runkon-flow-platform-spec.md`** — updated `ActionExecutor`, `ItemProvider`, `RunContext`, `GateResolver` trait snippets to new three-input shape; replaced `ExecutionContext` bundling struct description with three-input rationale; noted freeze carve-out; updated "What Stays in Each Layer"

## Commit

`26e131f8` — refactor(runkon-flow,conductor-core): wire &dyn RunContext into ActionExecutor + ItemProvider (#2850)
